// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "io.horiol.tictac"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
